{% extends 'base.html.twig' %}

{% block body %}
<div class="col-md-6 col-md-offset-2">

    <h1>User: <strong style="color: #002a80">{{ user.username }}</strong></h1>

    <div class="table-respovsive">
        <table class="table table-striped table-hover ">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ user.role }}</td>
            </tr>
        </tbody>
    </table>


            <a href="{{ path('user_edit', { 'id': user.id }) }}" class="btn btn-primary btn-sm">Edit</a>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger btn-sm">
            {{ form_end(delete_form) }}

    </div>
</div>
    <a href="{{ path('user_index') }}" class="btn btn-primary btn-sm">Back to the list</a>
{% endblock %}
